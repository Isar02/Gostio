name: gostio_desktop
description: Gostio desktop client for administrators and hosts.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  dio: ^5.11.0
  flutter:
    sdk: flutter
  json_annotation: ^4.12.0
  provider: ^6.1.5+1

dev_dependencies:
  build_runner: ^2.16.0
  flutter_lints: ^6.0.0
  json_serializable: ^6.14.1

flutter:
  uses-material-design: true
