name: gostio_core
description: The Gostio API contract, session and brand, shared by both clients.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.13.0

dependencies:
  dio: ^5.11.1
  flutter:
    sdk: flutter
  intl: ^0.20.2
  json_annotation: ^4.12.0

dev_dependencies:
  build_runner: ^2.16.1
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.14.1
